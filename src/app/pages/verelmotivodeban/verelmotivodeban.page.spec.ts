import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VerelmotivodebanPage } from './verelmotivodeban.page';

describe('VerelmotivodebanPage', () => {
  let component: VerelmotivodebanPage;
  let fixture: ComponentFixture<VerelmotivodebanPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(VerelmotivodebanPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
