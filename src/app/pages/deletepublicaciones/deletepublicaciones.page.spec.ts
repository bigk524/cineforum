import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DeletepublicacionesPage } from './deletepublicaciones.page';

describe('DeletepublicacionesPage', () => {
  let component: DeletepublicacionesPage;
  let fixture: ComponentFixture<DeletepublicacionesPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DeletepublicacionesPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
